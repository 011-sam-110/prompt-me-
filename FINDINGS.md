# Prompt Me — Findings Log

Append-only. One heading per audit pass (Round 2) or UX/logic critique pass (Round 3). Never delete a past entry — if a later pass re-finds something a previous pass thought was fixed, that oscillation needs to be visible here, not erased.

Format per entry:

```
## Round <2|3> — pass N (YYYY-MM-DD)
Agent: <who ran this pass>
Findings: <none | list>
Action: <none needed | what was fixed, with commit ref>
```

(No passes yet — this file is created empty at the start of Round 1.)
