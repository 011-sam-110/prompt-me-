{
  "name": "@prompt-me/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic types and logic shared by the web app and (later) the mobile port — see ENGINEERING_SPEC.md §1, §15.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./playback": {
      "types": "./src/clips/playback.ts",
      "default": "./src/clips/playback.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "music-metadata": "^11.15.0"
  }
}
