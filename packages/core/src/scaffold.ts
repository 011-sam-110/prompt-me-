/**
 * Placeholder module for the M1 scaffold milestone.
 *
 * packages/core is where the framework-agnostic match/date state machine
 * and feed-ranking logic will live (ENGINEERING_SPEC.md §1) — those land in
 * later milestones (M7, M6). This trivial export exists only so the
 * workspace's test command has something real to run against.
 */
export function ping(): "pong" {
  return "pong";
}
